version: '3.8'
services:
  streamingservice:
    build: .
    container_name: StreamingService
    ports:
      - "8091:8091"
    environment:
      - SPRING_PROFILES_ACTIVE=docker
      - UPLOAD_BASE_DIR=/app/uploads
    volumes:
      - ./data:/app/uploads
